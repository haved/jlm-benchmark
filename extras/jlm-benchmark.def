Bootstrap: docker
From: ubuntu:24.04

%files
    ./apt-install-dependencies.sh .

%post
    ./apt-install-dependencies.sh
    locale-gen en_US.UTF-8
    update-locale

    # Cleanup to reduce image size
    apt-get clean
